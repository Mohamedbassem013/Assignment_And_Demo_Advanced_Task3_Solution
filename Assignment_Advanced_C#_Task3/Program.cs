internal class Program
{
    private static void Main(string[] args)
    {
        #region Assignment ADV [02]

        #region Part [01]
        #region Question 01
        /*
             1)Given a string, find the first non-repeated character in it and return its index.
                If there is no such character, return -1. 
                (Hint you can use dictionary)
        */

        //Assume that Given string Like:⬇

        // Console.WriteLine("Please Enter a string and wait to get the first non repeated character in this string?");
        // string? userInput = Console.ReadLine();
        // 
        // if (userInput is not null)
        // {
        //     int indexOfTheNonRepeatedChar = Helper.FindTheFirstNonRepeatedCharIndex(userInput);
        // 
        //     Console.WriteLine($"The Index Of The Non Repeated Character is : {indexOfTheNonRepeatedChar}");
        // }
        #endregion

        #region Question 02
        //List<Book> books = new List<Book>
        //{
        //    new Book("1234", "Title of Book no.1", new string[] { "Author01" }, new DateTime(2010, 1, 1), 30m),
        //    new Book("5644", "Title of Book no.2", new string[] { "Author02", "Author03", "Author04", "Author05" }, new DateTime(1994, 1, 1), 55m),
        //    new Book("4166", "Title of Book no.3", new string[] { "Author06", "Author07" }, new DateTime(1999, 1, 1), 45m)
        //};


        //Console.WriteLine("Book Titles:");
        //LibraryEngine.ProcessBooks(books, BookFunctions.GetTitle);

        //Console.WriteLine("\nBook Authors:");
        //LibraryEngine.ProcessBooks(books, BookFunctions.GetAuthors);

        //Console.WriteLine("\nBook Prices:");
        //LibraryEngine.ProcessBooks(books, BookFunctions.GetPrice);

        #endregion
        #endregion

        #region Part [02]

        //var list = new List<int>();
        //list.Add(1);
        //list.Add(2);
        //list.Add(3);
        //list.Add(4);

        //bool exists = list.Exists(x => x > 2);  // true
        //int found = list.Find(x => x > 2);      // 3
        //List<int> foundAll = list.FindAll(x => x > 2);  // [3, 4]
        //int foundIndex = list.FindIndex(x => x > 2);    // 2
        //int foundLast = list.FindLast(x => x > 2);      // 4
        //int foundLastIndex = list.FindLastIndex(x => x > 2);  // 3
        //list.ForEach(Console.WriteLine);  // Prints 1, 2, 3, 4
        //bool trueForAll = list.TrueForAll(x => x > 0);  // true

        #endregion

        #endregion
    }
}